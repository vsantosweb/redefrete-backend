import styled from "@emotion/styled";

export const ServiceHeader = styled.header`
    display: flex;
    align-items: center;
    padding: ${({ theme }: any) => theme.defaultContainer.spacing}
`
export const ServiceHeaderLogoArea = styled.header`
`
